const BASE_PATH = '/'; // Update this to your desired base path
const origin = window.location.origin;
const view_screens = {
 
  SERVICES_DETAIL_VIEW: `${origin}/services_detail_view`,
 

};

export default view_screens;